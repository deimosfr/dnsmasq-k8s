## 1. Implementation
- [ ] 1.1 Update `chart/values.yaml` to include default `resources: {}` block
- [ ] 1.2 Update `chart/templates/statefulset.yaml` to include `resources` section using helm `toYaml`
- [ ] 1.3 Verify output with `helm template` to ensure resources are rendered correctly
